package com.hst.vendor.activity.fragmentactivity.cancelled;

import android.os.Bundle;
import android.support.annotation.Nullable;

import com.hst.vendor.languagesupport.BaseActivity;
import com.hst.vendor.R;

public class CancelledServiceDetailActivity extends BaseActivity {

    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_cancelled_service_detail);
    }
}
